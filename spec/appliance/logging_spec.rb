require 'spec_helper'

describe Appliance::Logging do
  it 'has a version number' do
    expect(Appliance::Logging::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
